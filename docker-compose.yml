version: "3.8"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: geo
      POSTGRES_PASSWORD: geo_dev
      POSTGRES_DB: geo_analyzer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
